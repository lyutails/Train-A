import { TrainConnectionsPipe } from './train-connections.pipe';

describe('TrainConnectionsPipe', () => {
  it('create an instance', () => {
    const pipe = new TrainConnectionsPipe();
    expect(pipe).toBeTruthy();
  });
});
