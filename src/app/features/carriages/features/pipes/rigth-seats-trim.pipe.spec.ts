import { RigthSeatsTrimPipe } from './rigth-seats-trim.pipe';

describe('RigthSeatsTrimPipe', () => {
  it('create an instance', () => {
    const pipe = new RigthSeatsTrimPipe();
    expect(pipe).toBeTruthy();
  });
});
